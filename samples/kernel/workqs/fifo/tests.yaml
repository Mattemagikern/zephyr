sample:
  description: FIFO work queue demo
  name: workq fifo demo
common:
  tags:
    - kernel
    - workqueue
tests:
  sample.kernel.workqs.fifo:
    build_only: true
    integration_platforms:
      - native_sim
